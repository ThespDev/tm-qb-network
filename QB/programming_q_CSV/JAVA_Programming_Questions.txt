JAVA Programming Questions

MC:
Which of the following is a primitive data type in Java?
    a) String
    b) Integer
    c) Double
    d) Boolean

MCA:
d) Boolean

MC:
What is the output of the following code?

int a = 10;
int b = 20;
System.out.println(a + b + " is the result");

a) 30 is the result
b) 1020 is the result
c) a + b is the result
d) None of the above

MCA:
b) 1020 is the result

MC:
Which of the following is a valid way to declare and initialize a String variable in Java?
    a) String s = "Hello";
    b) s = "Hello";
    c) String s;
    d) s = new String("Hello");

MCA:
a) String s = "Hello";

MC:
What is the correct syntax for a for loop in Java?
    a) for (int i = 0; i < 10; i++)
    b) for (i = 0; i < 10; i++)
    c) for (int i = 0; i < 10)
    d) for (i < 10; i++)

MCA:
a) for (int i = 0; i < 10; i++)

MC:
What is the purpose of the keyword "static" in Java?
    a) It is used to create an instance of a class
    b) It is used to declare a variable as constant
    c) It is used to access a method or variable without creating an object of the class
    d) It is used to declare a method as private

MCA:
c) It is used to access a method or variable without creating an object of the class

Q:
What is the keyword used to define a constant variable in Java?

A:
The keyword used to define a constant variable in Java is "final".

Q:
What is the difference between a while loop and a do-while loop in Java?

A:
The difference between a while loop and a do-while loop in Java is that a while loop checks the condition before entering the loop, while a do-while loop checks the condition after executing the loop once.

Q:
What is the difference between an interface and a class in Java?

A:
The difference between an interface and a class in Java is that an interface defines a set of methods that a class must implement, while a class is a blueprint for creating objects.

Q:
What is the purpose of the "static" keyword in Java?

A:
The "static" keyword in Java is used to create class-level variables and methods that can be accessed without creating an instance of the class.

Q:
What is the difference between an ArrayList and a LinkedList in Java?

A:
The difference between an ArrayList and a LinkedList in Java is that an ArrayList uses an array to store elements and provides constant-time access to the elements, while a LinkedList uses a linked list to store elements and provides constant-time insertion and deletion of elements.




