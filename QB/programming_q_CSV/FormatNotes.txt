 In text commas are represented as <comma> and line breaks represented as <br>

 *also ">" delinates start of code in CSV file, so format differently when displaying to user

MCA = Multi Choice A (I forgot what a stands for)
        Can take 4 max multi choice options
        Answers last field in number form (e.g. A=1,B=2 e.t.c)
Code = Questions designed to be execed in progran
        Can take 3 arguments expected to go in
        And then 3 expected outputs once execed
